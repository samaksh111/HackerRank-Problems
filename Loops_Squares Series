#include <stdio.h>
int main() {
    int num;
    scanf("%d", &num);
    for (int i =1; i<= num ;i++)
    {
        if(i*i %3 !=0)
        printf("%d ", i*i);
        else
            num++;
    }
    
}